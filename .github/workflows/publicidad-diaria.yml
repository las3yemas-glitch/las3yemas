name: Publicidad diaria Las 3 Yemas

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publicar-instagram:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Crear imagen nueva
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick

          convert publicidad/plantilla-las3yemas.png \
            -resize 1080x1350! \
            -quality 95 \
            publicidad/instagram-${GITHUB_RUN_ID}.jpg

          file publicidad/instagram-${GITHUB_RUN_ID}.jpg

      - name: Guardar imagen
        run: |
          git config user.name "Las 3 Yemas Bot"
          git config user.email "bot@las3yemas.cl"
          git add publicidad/instagram-${GITHUB_RUN_ID}.jpg
          git commit -m "Nueva publicidad Instagram" || true
          git push

      - name: Esperar imagen pública
        run: |
          IMAGE_URL="https://las3yemas-glitch.github.io/las3yemas/publicidad/instagram-${GITHUB_RUN_ID}.jpg"

          for i in {1..30}; do
            STATUS=$(curl -L -s -o /dev/null -w "%{http_code}" "$IMAGE_URL")
            echo "Intento $i: HTTP $STATUS"

            if [ "$STATUS" = "200" ]; then
              echo "IMAGEN NUEVA DISPONIBLE"
              exit 0
            fi

            sleep 5
          done

          exit 1

      - name: Publicar en Instagram
        env:
          ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
        run: |
          IMAGE_URL="https://las3yemas-glitch.github.io/las3yemas/publicidad/instagram-${GITHUB_RUN_ID}.jpg"

          IG_USER_ID=$(curl -s \
            "https://graph.instagram.com/me?fields=id&access_token=${ACCESS_TOKEN}" \
            | python3 -c "import sys,json; print(json.load(sys.stdin).get('id',''))")

          RESPONSE=$(curl -s -X POST \
            "https://graph.instagram.com/${IG_USER_ID}/media" \
            -d "image_url=${IMAGE_URL}" \
            --data-urlencode "caption=🥚 Huevos frescos directo a tu hogar. 💗 Calidad, frescura y confianza. 🚚 Repartimos en Viña del Mar, Valparaíso, Quilpué, Belloto y Villa Alemana. #Las3Yemas #HuevosFrescos" \
            -d "access_token=${ACCESS_TOKEN}")

          echo "$RESPONSE"

          CREATION_ID=$(echo "$RESPONSE" | python3 -c "import sys,json; print(json.load(sys.stdin).get('id',''))")

          if [ -z "$CREATION_ID" ]; then
            exit 1
          fi

          sleep 10

          curl -s -X POST \
            "https://graph.instagram.com/${IG_USER_ID}/media_publish" \
            -d "creation_id=${CREATION_ID}" \
            -d "access_token=${ACCESS_TOKEN}"
